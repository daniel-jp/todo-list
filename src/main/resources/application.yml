spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET:504E635266556A586E3272357538782F413F4428472B4B6250645367566B6570}
      expiration: 36000000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      show-details: always

  info:
    env:
      enabled: true

info:
  application:
    name: todo-list
    description: A todo-list with articles about The Spring Framework.
    author: Danny Paulino
    version: 1.0.0