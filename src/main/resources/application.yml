
management:
  endpoints:
    web:
      exposure:
        include: '*'
  info:
    env:
      enabled: true

info:
  application:
    name: todo-list
    description: A TODO LIST  with articles about The Spring Framework.
    author: Danny paulino
    version: 1.0.0
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}